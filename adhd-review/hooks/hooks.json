{
  "description": "Inject the adhd-review output style into the main session. On by default; set CLAUDE_ADHD_REVIEW=0 to disable it for a session.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/session-start.sh\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
