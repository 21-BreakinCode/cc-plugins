{
  "description": "On Stop, audit the just-finished turn's FACT/completion claims against the transcript's real tool calls. On by default (mode: warn — flags unbacked claims; CLAUDE_RECEIPTS_MODE=block or /receipts block hard-gates them). Set CLAUDE_RECEIPTS=0 to disable.",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/receipts-check.sh\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
